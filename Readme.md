computer program- is a sequence of  instructions that directs a computer to perform certain actions in a specified order.
programming languge, language designed to  facilitate the writing of  instructions for computers.
Programming - the act of writting a computer program
programs are executed in the computer hardware, physical components of a computer.
softwarre-programs on a system that are designed to be eecuted on hardware.
programs that can be easily transferred fromone platform to another is said to be portable
porting- the act of modifying a program so that it runs on a different platform
CPUs are only capable or  processing instructions written in machine languae.A set o f all possible machine language instructions that a given CPU can understand is called an Instruction set.
C++ excels in situations where high performance and precise control over memory and other resources is needed.here are a few types of aplicaions that C++ would excel in:
  video games
  realtime systems(for transportation, manufacturing, etc...)
  high performance financial applications(e.g. high frequency trading)
//Syntax and syntax errors
syntax is a set of rules tha describe how specific words and punctuation can be arranged in a to form valid sentences in a language
syntax errors  occur when your program violate the grammar rules of the C++ language
A statement is an instruction in a computer program that tells the computer to perform an action
a function is a collection of statements that executes sequentially.
All C++ programs must have the main() function in order to run
When a program is run the statements inside the main funtion  are execcuted in sequential order

A library file is a collection of precompiled code that has been "packaged up" for reuse in other programs. The C++ Standard library is a library the ships with C++. It contains additonal functionality touse in your programs.